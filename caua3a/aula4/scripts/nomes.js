let nomes = ['Dimas', 'Pedro', 'Caua', 'Juan', 'Catapimbas'];

for (let i = 0; i < nomes.length; i++) {
    let finder = nomes[i];

    if (finder = 'Caua'){
        alert('Caua encontrado');
    } else {
        alert('A procura de Caua');
    };
    
}